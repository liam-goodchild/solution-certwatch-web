skip-check:
  - CKV_GHA_7 # workflow_dispatch inputs are intentional (plan/apply/destroy selection)
  - CKV_AZURE_111 # Key Vault enables soft delete
  - CKV_AZURE_42 # Key Vault is recoverable
  - CKV_AZURE_110 # Key Vault enables purge protection
  - CKV_AZURE_182 # VNET has at least 2 connected DNS Endpoints
  - CKV_AZURE_9 # RDP access restricted from the internet
  - CKV_AZURE_160 # HTTP (port 80) access restricted from the internet
  - CKV_AZURE_10 # SSH access restricted from the internet
  - CKV_AZURE_77 # UDP services restricted from the internet
  - CKV2_AZURE_31 # VNET subnet configured with NSG
  - CKV_AZURE_189 # Key Vault disables public network access
  - CKV_AZURE_109 # Key Vault allows firewall rules settings
  - CKV2_AZURE_32 # Private endpoint configured for Key Vault
  - CKV_AZURE_100 # Ensure that Cosmos DB accounts have customer-managed keys to encrypt data at rest
  - CKV_AZURE_140 # Ensure that Local Authentication is disabled on CosmosDB
  - CKV_AZURE_101 # Ensure that Azure Cosmos DB disables public network access
  - CKV_AZURE_99 # Ensure Cosmos DB accounts have restricted access
  - CKV_AZURE_132 # Ensure cosmosdb does not allow privileged escalation by restricting management plane changes
  # Functions storage account - intentional trade-offs for Azure Functions Flex Consumption
  - CKV_AZURE_33 # Queue service logging not required for Functions deployment storage
  - CKV_AZURE_206 # LRS replication is intentional for low-cost deployment storage
  - CKV_AZURE_212 # Min instances not applicable to FC1 Flex Consumption plan
  - CKV_AZURE_225 # Zone redundancy not required for this workload
  - CKV2_AZURE_41 # SAS expiration policy not applicable; using managed identity auth
  - CKV2_AZURE_1 # CMK encryption not required for Functions deployment storage
  - CKV2_AZURE_33 # Private endpoint not configured; access via managed identity
  - CKV2_AZURE_21 # Blob service read logging not required for deployment container
  # Static Web App auth config - clientSecretSettingName is an app setting name, not a secret value
  - CKV_SECRET_6
